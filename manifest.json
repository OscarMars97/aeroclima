/* Configuración local segura. Para usar Unsplash o Pexels, configura un proxy propio;
   nunca agregues una clave de API directamente en este archivo. */
window.AEROCLIMA_CONFIG = {
  imageProvider: 'local',
  imageApiEndpoint: '',
  imageProxyEndpoint: '',
  reduceData: false,
  animations: true
};
